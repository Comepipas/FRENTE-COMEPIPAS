document.addEventListener('DOMContentLoaded',async()=>{const box=document.querySelector('#publicTravels'),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),fmt=v=>v?new Date(v).toLocaleString('es-ES',{dateStyle:'full',timeStyle:'short'}):'Fecha por confirmar',duration=m=>m?`${Math.floor(m/60)} h ${m%60?`${m%60} min`:''}`:'Por calcular';try{const trips=await OnTourV25.trips(false);if(!trips.length){box.innerHTML=`<section class="on-tour-empty"><img src="assets/images/on-tour/on-tour-preparando.png" alt="Preparando el siguiente ON TOUR"><h2>🚐 Preparando el siguiente ON TOUR...</h2><p>Mientras esperamos el próximo desplazamiento...</p><div class="on-tour-empty-list"><span>🛞 Inflando las ruedas</span><span>⛽ Llenando el depósito</span><span>🧣 Preparando las bufandas</span><span>🎺 Afinando los cánticos</span><span>🍺 Enfriando las cervezas para la previa</span></div><h3>📍 Próximo destino: ???</h3><p>Muy pronto anunciaremos un nuevo ON TOUR.</p></section>`;return}box.innerHTML=trips.map(t=>`<article class="on-tour-card"><div class="on-tour-card-cover">${t.imagen_url||t.escudo_url?`<img src="${esc(t.imagen_url||t.escudo_url)}" alt="${esc(t.rival||t.titulo)}">`:`<img src="assets/images/on-tour/on-tour-preparando.png" alt="ON TOUR">`}</div><div class="on-tour-card-body"><span class="on-tour-badge">${esc(t.estado)}</span><h3>${esc(t.titulo)}</h3><div class="on-tour-destination"><span>📍 Próximo destino</span><strong>🏟️ ${esc(t.estadio||t.destino)}</strong><span>${esc(t.ciudad||'')}</span><span>🛣️ ${t.km_desde_rosaleda!=null?`${t.km_desde_rosaleda} km desde La Rosaleda`:'Distancia por confirmar'}</span><span>⏱️ ${duration(t.duracion_minutos)}</span></div><p>${fmt(t.fecha_salida)}</p><p>${esc(t.descripcion||'')}</p><a class="btn btn-primary" href="area-socio.html">Solicitar entrada</a><p class="privacy-note">La solicitud no garantiza la obtención de entradas. La adjudicación dependerá de las entradas facilitadas por el rival y del número de solicitudes.</p></div></article>`).join('')}catch(e){box.innerHTML=`<p>${esc(e.message)}</p>`}});
