(()=>{"use strict";const S=window.OnTourV25,$=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),fmt=v=>v?new Date(v).toLocaleString('es-ES',{dateStyle:'full',timeStyle:'short'}):'—';let trips=[],mine=[];async function load(){try{[trips,mine]=await Promise.all([S.trips(false),S.myBookings()]);render()}catch(e){const x=$('#memberTripsBody');if(x)x.innerHTML=`<tr><td colspan="5">${esc(e.message)}</td></tr>`}}function render(){const body=$('#memberTripsBody');if(body)body.innerHTML=mine.length?mine.map(r=>`<tr><td>${esc(r.travel_events?.titulo||'ON TOUR')}</td><td>${fmt(r.travel_events?.fecha_salida)}</td><td>${r.total_plazas}</td><td>${esc(r.estado)}</td><td>Solicitud de entradas</td></tr>`).join(''):'<tr><td colspan="5">Todavía no tienes solicitudes ON TOUR.</td></tr>';const box=$('#memberTravelAvailable');if(!box)return;if(!trips.length){box.innerHTML='<p>Estamos preparando el siguiente ON TOUR.</p>';return}box.innerHTML=trips.map(t=>`<article class="on-tour-card"><div class="on-tour-card-body"><span class="on-tour-badge">${esc(t.estado)}</span><h3>${esc(t.titulo)}</h3><div class="on-tour-destination"><strong>🏟️ ${esc(t.estadio||t.destino)}</strong><span>🛣️ ${t.km_desde_rosaleda!=null?`${t.km_desde_rosaleda} km desde La Rosaleda`:'Distancia por confirmar'}</span><span>${fmt(t.fecha_salida)}</span></div>${t.estado==='abierto'?`<button class="btn btn-primary" data-request-on-tour="${t.id}">Solicitar entrada</button>`:'<span class="on-tour-badge">Solicitudes cerradas</span>'}</div></article>`).join('');document.querySelectorAll('[data-request-on-tour]').forEach(b=>b.onclick=()=>openRequest(b.dataset.requestOnTour))}function ensureModal(){if($('#onTourRequestModal'))return;document.body.insertAdjacentHTML('beforeend',`<div id="onTourRequestModal" class="on-tour-modal"><form id="onTourRequestForm" class="on-tour-dialog on-tour-request-dialog"><button type="button" data-on-tour-close class="modal-x">×</button><h2>Solicitud ON TOUR</h2><input type="hidden" name="travel_id"><div class="request-warning"><strong>IMPORTANTE</strong><p>Enviar esta solicitud no da derecho a obtener entradas. La adjudicación dependerá de las entradas facilitadas por el club rival y del número total de solicitudes recibidas.</p></div><div class="on-tour-form"><label>Tu nombre y apellidos<input name="solicitante_nombre" required></label><label>Tu DNI<input name="solicitante_dni" required autocomplete="off"></label><label>Tu número de abonado del Málaga CF<input name="solicitante_abonado" required autocomplete="off"></label><label>Número de acompañantes<select name="companion_count"><option value="0">Ninguno</option><option value="1">1 acompañante</option><option value="2">2 acompañantes</option><option value="3">3 acompañantes</option></select></label></div><div id="onTourCompanions"></div><p class="privacy-note">Los datos se introducen manualmente y se verifican de forma privada. La web nunca muestra ni autocompleta los datos de otros socios.</p><label><input type="checkbox" name="accept" required> He leído y acepto que esta solicitud no garantiza la obtención de entradas.</label><label style="display:grid;gap:6px;margin-top:12px">Observaciones<textarea name="notes" rows="3"></textarea></label><p id="onTourRequestMessage"></p><button class="btn btn-primary" type="submit">Enviar solicitud</button></form></div>`);$('#onTourRequestForm').companion_count.onchange=renderCompanions;$('[data-on-tour-close]').onclick=()=>$('#onTourRequestModal').classList.remove('open');$('#onTourRequestForm').onsubmit=submitRequest}function renderCompanions(){const n=Number($('#onTourRequestForm').companion_count.value),box=$('#onTourCompanions');box.innerHTML=Array.from({length:n},(_,i)=>`<section class="companion-row"><h3>Acompañante ${i+1}</h3><div class="on-tour-form"><label>Nombre y apellidos<input name="companion_name_${i}" required></label><label>DNI<input name="companion_dni_${i}" required autocomplete="off"></label><label class="full">Número de abonado del Málaga CF<input name="companion_abonado_${i}" required autocomplete="off"></label></div></section>`).join('')}function openRequest(id){ensureModal();const f=$('#onTourRequestForm');f.reset();f.travel_id.value=id;renderCompanions();$('#onTourRequestMessage').textContent='';$('#onTourRequestModal').classList.add('open')}async function submitRequest(e){e.preventDefault();const f=e.currentTarget,n=Number(f.companion_count.value),companions=[];for(let i=0;i<n;i++)companions.push({nombre:f[`companion_name_${i}`].value,dni:f[`companion_dni_${i}`].value,numero_abonado:f[`companion_abonado_${i}`].value});const applicant={nombre:f.solicitante_nombre.value,dni:f.solicitante_dni.value,numero_abonado:f.solicitante_abonado.value};const msg=$('#onTourRequestMessage');msg.textContent='Verificando datos…';try{await S.request(f.travel_id.value,applicant,companions,f.notes.value,f.accept.checked);msg.textContent='Solicitud registrada correctamente. Recuerda que no garantiza la asignación de entradas.';setTimeout(()=>{$('#onTourRequestModal').classList.remove('open');load()},1600)}catch(err){msg.textContent=err.message}}document.addEventListener('DOMContentLoaded',()=>{ensureModal();load()})})();
