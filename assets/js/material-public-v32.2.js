(()=>{
  "use strict";
  const S=MaterialV20,$=s=>document.querySelector(s);let items=[],cats=[];
  const esc=v=>String(v??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));
  const money=v=>new Intl.NumberFormat("es-ES",{style:"currency",currency:"EUR"}).format(Number(v||0));
  const status=v=>{v=String(v||"").toLowerCase();return v.includes("no disponible")?"unavailable":v.includes("poca")?"low":"available"};
  const photos=i=>i.imagenes?.length?i.imagenes:(i.imagen_url?[{imagen_url:i.imagen_url,principal:true}]:[]);
  const mainPhoto=i=>photos(i)[0]?.imagen_url||"";
  function card(i){const disabled=i.disponibilidad==="No disponible",photo=mainPhoto(i);return `<article class="m322-card${disabled?" is-disabled":""}" data-product-id="${esc(i.id)}" tabindex="${disabled?-1:0}"><div class="m322-card-media">${photo?`<img src="${esc(photo)}" alt="${esc(i.nombre)}" loading="lazy">`:`<span>${esc(i.emoji||"📦")}</span>`}<div class="m322-flags">${i.nuevo?'<b class="new">Nuevo</b>':''}${i.destacado?'<b class="featured">Destacado</b>':''}</div></div><div class="m322-card-body"><span class="m322-badge ${status(i.disponibilidad)}">${esc(i.disponibilidad||"Consultar")}</span><h3>${esc(i.nombre)}</h3>${i.mostrar_precio&&i.precio!=null?`<strong class="m322-price">${money(i.precio)}</strong>`:'<strong class="m322-price muted">Consultar precio</strong>'}<p>${esc(i.descripcion||"Material de la Peña Frente Comepipas.")}</p><div class="m322-quick-meta">${i.material_categories?.nombre?`<span>📂 ${esc(i.material_categories.nombre)}</span>`:""}${i.color?`<span>🎨 ${esc(i.color)}</span>`:""}${i.variantes?`<span>📏 ${esc(i.variantes)}</span>`:""}</div><button class="btn btn-primary" data-interest="${esc(i.id)}" ${disabled?"disabled":""}>${disabled?"No disponible":"Solicitar"}</button></div></article>`}
  function render(){const q=$("#m20Search").value.toLowerCase().trim(),c=$("#m20Category").value;const f=items.filter(i=>(!c||i.categoria_id===c)&&(!q||`${i.nombre} ${i.descripcion||""} ${i.variantes||""} ${i.color||""}`.toLowerCase().includes(q)));$("#m20Grid").innerHTML=f.length?f.map(card).join(""):'<div class="m322-empty">No hay material con esos filtros.</div>'}
  function productHtml(i){const imgs=photos(i),hero=imgs[0]?.imagen_url||"";return `<div class="m322-product-grid"><div><div class="m322-product-main">${hero?`<img id="m322MainPhoto" src="${esc(hero)}" alt="${esc(i.nombre)}">`:`<span>${esc(i.emoji||"📦")}</span>`}</div>${imgs.length>1?`<div class="m322-thumbs">${imgs.map((x,n)=>`<button class="${n===0?"active":""}" data-thumb="${esc(x.imagen_url)}"><img src="${esc(x.imagen_url)}" alt="Vista ${n+1}"></button>`).join("")}</div>`:""}</div><div class="m322-product-info"><div class="m322-flags static">${i.nuevo?'<b class="new">Nuevo</b>':''}${i.destacado?'<b class="featured">Destacado</b>':''}</div><span class="m322-badge ${status(i.disponibilidad)}">${esc(i.disponibilidad)}</span><h2>${esc(i.nombre)}</h2>${i.mostrar_precio&&i.precio!=null?`<div class="m322-product-price">${money(i.precio)}</div>`:'<div class="m322-product-price muted">Consultar precio</div>'}<p>${esc(i.descripcion||"")}</p><dl>${i.material_categories?.nombre?`<div><dt>Categoría</dt><dd>${esc(i.material_categories.nombre)}</dd></div>`:""}${i.color?`<div><dt>Color</dt><dd>${esc(i.color)}</dd></div>`:""}${i.variantes?`<div><dt>Tallas o variantes</dt><dd>${esc(i.variantes)}</dd></div>`:""}</dl><p class="m322-notice">Este catálogo es informativo. No realizamos venta online.</p><button class="btn btn-primary" data-interest="${esc(i.id)}" ${i.disponibilidad==="No disponible"?"disabled":""}>${i.disponibilidad==="No disponible"?"No disponible":"Solicitar"}</button></div></div>`}
  function openProduct(id){const i=items.find(x=>String(x.id)===String(id));if(!i)return;$("#m322ProductContent").innerHTML=productHtml(i);$("#m322ProductDialog").showModal()}
  function openRequest(id){const i=items.find(x=>String(x.id)===String(id));if(!i||i.disponibilidad==="No disponible")return;$("#m322ProductDialog").close();const f=$("#m20RequestForm");f.reset();f.material_id.value=i.id;$("#m20Summary").innerHTML=`<div class="m322-request-summary">${mainPhoto(i)?`<img src="${esc(mainPhoto(i))}" alt="${esc(i.nombre)}">`:""}<div><h3>${esc(i.nombre)}</h3>${i.mostrar_precio&&i.precio!=null?`<strong>${money(i.precio)}</strong>`:""}${i.variantes?`<p>Tallas o variantes: ${esc(i.variantes)}</p>`:""}</div></div>`;$("#m20RequestDialog").showModal()}
  async function load(){try{[cats,items]=await Promise.all([S.categories(),S.items()]);$("#m20Category").innerHTML='<option value="">Todas</option>'+cats.map(c=>`<option value="${esc(c.id)}">${esc(c.nombre)}</option>`).join("");render()}catch(e){$("#m20Grid").innerHTML=`<div class="m322-empty">No se pudo cargar el catálogo: ${esc(e.message)}</div>`}}
  $("#m20Grid").addEventListener("click",e=>{const id=e.target.closest("[data-interest]")?.dataset.interest;if(id){e.stopPropagation();openRequest(id);return}const card=e.target.closest("[data-product-id]");if(card)openProduct(card.dataset.productId)});
  $("#m20Grid").addEventListener("keydown",e=>{if((e.key==="Enter"||e.key===" ")&&e.target.matches("[data-product-id]")){e.preventDefault();openProduct(e.target.dataset.productId)}});
  $("#m322ProductDialog").onclick=e=>{if(e.target.matches("[data-close-product]"))$("#m322ProductDialog").close();const t=e.target.closest("[data-thumb]");if(t){$("#m322MainPhoto").src=t.dataset.thumb;document.querySelectorAll("[data-thumb]").forEach(x=>x.classList.toggle("active",x===t))}const b=e.target.closest("[data-interest]");if(b)openRequest(b.dataset.interest);if(e.target.id==="m322MainPhoto"){$("#m322LightboxImage").src=e.target.src;$("#m322Lightbox").showModal()}};
  $("#m322Lightbox").onclick=e=>{if(e.target.matches("[data-close-lightbox]")||e.target===$("#m322Lightbox"))$("#m322Lightbox").close()};
  $("#m20RequestForm").onsubmit=async e=>{if(e.submitter?.value==="cancel")return;e.preventDefault();try{const r=await S.submit(Object.fromEntries(new FormData(e.currentTarget)));$("#m20RequestDialog").close();$("#m20Confirm").innerHTML=`<p>Referencia: <b>${esc(r.referencia)}</b></p><p>Hemos recibido tu solicitud. Nos pondremos en contacto contigo.</p>`;$("#m20ConfirmDialog").showModal()}catch(x){alert(x.message)}};
  $("#m20CloseConfirm").onclick=()=>$("#m20ConfirmDialog").close();$("#m20Search").oninput=render;$("#m20Category").onchange=render;load();
})();
