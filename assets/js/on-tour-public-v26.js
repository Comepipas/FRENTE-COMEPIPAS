document.addEventListener('DOMContentLoaded',async()=>{
const box=document.querySelector('#publicTravels'),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),fmt=v=>v?new Date(v).toLocaleString('es-ES',{dateStyle:'full',timeStyle:'short'}):'Fecha por confirmar',duration=m=>m?`${Math.floor(m/60)} h ${m%60?`${m%60} min`:''}`:'Por calcular',normal=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
try{const trips=await OnTourV25.trips(false);if(!trips.length){box.innerHTML='<section class="on-tour-empty"><img src="assets/images/on-tour/on-tour-preparando.png" alt="Preparando el siguiente ON TOUR"><h2>Preparando el siguiente ON TOUR</h2><p>Muy pronto anunciaremos un nuevo desplazamiento.</p></section>';return}box.innerHTML=trips.map(t=>{const madrid=normal(`${t.rival||''} ${t.ciudad||''}`).includes('madrid'),estadio=madrid?'assets/images/stadiums/santiago-bernabeu-v2.png':t.imagen_url||t.escudo_url||'assets/images/on-tour/on-tour-preparando.png',ciudad=madrid?'assets/images/cities/madrid.png':'';return `<article class="on-tour-card"><div class="on-tour-card-cover"><img src="${esc(estadio)}" alt="${esc(t.estadio||t.rival||t.titulo)}">${ciudad?`<img class="on-tour-city-photo" src="${ciudad}" alt="Vista de Madrid">`:''}</div><div class="on-tour-card-body"><span class="on-tour-badge">${esc(t.estado)}</span><h3>${esc(t.titulo)}</h3><div class="on-tour-destination"><span>Proximo destino</span><strong>${esc(t.estadio||t.destino)}</strong><span>${esc(t.ciudad||'')}</span><span>${t.km_desde_rosaleda!=null?`${t.km_desde_rosaleda} km desde La Rosaleda`:'Distancia por confirmar'}</span><span>${duration(t.duracion_minutos)}</span></div><p>${fmt(t.fecha_salida)}</p><p>${esc(t.descripcion||'')}</p><a class="btn btn-primary on-tour-request-cta" href="area-socio.html?on_tour=${encodeURIComponent(t.id)}#trips">Solicitar entradas ahora</a><p class="privacy-note">La solicitud no garantiza la entrada. La directiva comunicara posteriormente la adjudicacion.</p></div></article>`}).join('')}catch(e){box.innerHTML=`<p>${esc(e.message)}</p>`}
});
