document.addEventListener('DOMContentLoaded',async()=>{const box=document.querySelector('#publicTravels'),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),fmt=v=>v?new Date(v).toLocaleString('es-ES',{dateStyle:'full',timeStyle:'short'}):'Fecha por confirmar',duration=m=>m?`${Math.floor(m/60)} h ${m%60?`${m%60} min`:''}`:'Por calcular',status=t=>{if(t.cierre&&Date.now()>new Date(t.cierre).getTime())return'plazo finalizado';if(t.estado==='completo'||Number(t.plazas_totales||0)-Number(t.plazas_ocupadas||0)<=0)return'completo';return t.estado};try{const trips=await OnTourV25.trips(false);if(!trips.length){box.innerHTML=`<section class="on-tour-empty"><img src="assets/images/on-tour/on-tour-preparando.png" alt="Preparando el siguiente ON TOUR"><h2>🚐 Preparando el siguiente ON TOUR...</h2><p>Muy pronto anunciaremos un nuevo desplazamiento.</p></section>`;return}box.innerHTML=trips.map(t=>{const st=status(t),map=t.maps_url||`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent([t.estadio,t.ciudad].filter(Boolean).join(', '))}`;return`<article class="on-tour-card"><div class="on-tour-card-cover"><img src="${esc(t.imagen_url||t.escudo_url||'assets/images/on-tour/on-tour-preparando.png')}" alt="${esc(t.rival||t.titulo)}"></div><div class="on-tour-card-body"><span class="on-tour-badge status-${esc(st.replace(/\s+/g,'-'))}">${esc(st)}</span><h3>${esc(t.titulo)}</h3><div class="on-tour-destination"><span>📍 Próximo destino</span><strong>🏟️ ${esc(t.estadio||t.destino)}</strong><span>${esc(t.ciudad||'')}</span><span>🛣️ ${t.km_desde_rosaleda!=null?`${t.km_desde_rosaleda} km desde La Rosaleda`:'Distancia por confirmar'}</span><span>⏱️ ${duration(t.duracion_minutos)}</span></div><p>${fmt(t.fecha_salida)}</p><p>${esc(t.descripcion||'')}</p><div class="on-tour-actions"><a class="btn btn-primary" href="area-socio.html">Solicitar entrada</a><a class="btn btn-secondary" href="${esc(map)}" target="_blank" rel="noopener">Cómo llegar</a></div><p class="privacy-note">La solicitud no garantiza la obtención de entradas. La adjudicación dependerá de las entradas facilitadas por el rival.</p></div></article>`}).join('');box.querySelectorAll('img').forEach(img=>img.onerror=()=>{img.onerror=null;img.src='assets/images/on-tour/on-tour-preparando.png'})}catch(e){box.innerHTML=`<p>${esc(e.message)}</p>`}});
