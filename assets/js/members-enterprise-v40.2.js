(()=>{'use strict';const $=s=>document.querySelector(s);const norm=v=>String(v||'').toLowerCase();function setup(){const date=$('#moduleDate');if(date)date.textContent=new Intl.DateTimeFormat('es-ES',{weekday:'long',day:'numeric',month:'long'}).format(new Date());$('#sidebarOpen')?.addEventListener('click',()=>$('#enterpriseSidebar').classList.add('open'));$('#sidebarClose')?.addEventListener('click',()=>$('#enterpriseSidebar').classList.remove('open'));document.querySelector('[data-close-manager]')?.addEventListener('click',()=>$('#memberManagerModal').classList.remove('open'));const input=$('#moduleSearch'),results=$('#searchResults'),links=[...document.querySelectorAll('.enterprise-nav a')];input?.addEventListener('input',()=>{const q=norm(input.value.trim());if(!q){results.classList.remove('open');return}const found=links.filter(a=>norm(a.textContent).includes(q)).slice(0,7);results.innerHTML=found.length?found.map(a=>`<a href="${a.getAttribute('href')}">${a.textContent.trim()}</a>`).join(''):'<span class="empty-state">Sin resultados</span>';results.classList.add('open')});document.addEventListener('click',e=>{if(!e.target.closest('.topbar-search'))results?.classList.remove('open')})}document.addEventListener('DOMContentLoaded',setup)})();
