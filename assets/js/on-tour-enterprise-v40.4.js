(()=>{'use strict';const $=s=>document.querySelector(s),norm=v=>String(v||'').toLowerCase();document.addEventListener('DOMContentLoaded',()=>{const d=$('#moduleDate');if(d)d.textContent=new Intl.DateTimeFormat('es-ES',{weekday:'long',day:'numeric',month:'long'}).format(new Date());$('#sidebarOpen')?.addEventListener('click',()=>$('#enterpriseSidebar').classList.add('open'));$('#sidebarClose')?.addEventListener('click',()=>$('#enterpriseSidebar').classList.remove('open'));const input=$('#moduleSearch'),results=$('#searchResults'),links=[...document.querySelectorAll('.enterprise-nav a')];input?.addEventListener('input',()=>{const q=norm(input.value.trim());if(!q)return results.classList.remove('open');const found=links.filter(a=>norm(a.textContent).includes(q)).slice(0,7);results.innerHTML=found.map(a=>`<a href="${a.href}">${a.textContent.trim()}</a>`).join('')||'<span class="empty-state">Sin resultados</span>';results.classList.add('open')});document.addEventListener('click',e=>{if(!e.target.closest('.topbar-search'))results?.classList.remove('open')})})})();
