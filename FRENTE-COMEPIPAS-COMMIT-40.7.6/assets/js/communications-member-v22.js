document.addEventListener('DOMContentLoaded',()=>{let box=document.querySelector('#c22MemberNotices');if(!box)return;const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));async function load(){try{let rows=await CommsV22.myNotices();box.innerHTML=rows.length?rows.map(x=>`<article class="c22-notice ${x.importante?'important':''} ${x.leido_at?'':'unread'}" data-id="${x.id}"><div class="c22-actions"><strong>${esc(x.titulo)}</strong><small>${new Date(x.created_at).toLocaleString('es-ES')}</small></div><div>${x.cuerpo_html}</div>${x.leido_at?'':'<button class="btn btn-dark-outline" data-read>Marcar como leído</button>'}</article>`).join(''):'<p>No tienes avisos nuevos.</p>';box.querySelectorAll('[data-read]').forEach(b=>b.onclick=async()=>{await CommsV22.readNotice(b.closest('[data-id]').dataset.id);load()})}catch(e){box.innerHTML=`<p>No se pudieron cargar los avisos: ${esc(e.message)}</p>`}}load()});
